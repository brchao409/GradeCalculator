import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({

    addButton: {
        margin: 0,
        top: 'auto',
        right: 20,
        bottom: 20,
        left: 'auto',
        position: 'fixed',
    },

}));